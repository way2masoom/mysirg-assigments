// Write a program to print the first 10 even natural numbers.
#include <stdio.h>
int main()
{
    int i = 1;
    // int value = 0;
    while (i <= 10)
    {
        // value = i * 2;
        printf("Even Natural Number=%d\n", i * 2);
        i++;
    }

    return 0;
}