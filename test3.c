#include <stdio.h>
#include <conio.h>

int main()
{
    // Taking value Using getch()

    int x = getch(); // getch() only take single value

    printf("You Enter %d", x);

    return 0;
}
