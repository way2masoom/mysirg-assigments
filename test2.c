//
#include <stdio.h>

int main()
{
    int a = -2, b = +2;
    printf("% d, % d, % d", a = a + b, b = a - b, a + b);

    // printf("%d", a = a + b);
    // printf("%d", b = a - b);
    // printf("%d", a + b);
    return 0;
}