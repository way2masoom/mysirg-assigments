// Write a C program to print character stored in a char variable, also print its ASCII code.
// Write a C program to print character stored in
// a char variable, also print its ASCII code.
#include <stdio.h>
#include <stdio.h>
int main()
{
    char strType1 = 'A';
    char strType2 = 'a';
    printf("This is ASCII Code of A and a = \t %d & %d", strType1, strType2);

    // getch();
    return 0;
}